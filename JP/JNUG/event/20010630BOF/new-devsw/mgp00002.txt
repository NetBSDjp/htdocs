
最終目標

Kernel Dynamic Configuration フレームワークの実装

簡単に言うと、カーネルの再コンパイルなしにドライバを足したり引いたりなどができる仕組み。FreeBSD にある kldload/kldunload のようなやつ

難しくいうと、config(8) が現在内部に持っている files 関係のメタ情報をconfig(8) 終了時にも破棄せずに、kernel もしくは userland に保持し、いつでも動的に追加／変更などが行えるようにする仕組み。
（これは FreeBSD にはない。ドライバドリブンでやるから）

胴体だけで起動して、あとから手や足を付け足すようなイメージ。
合体ロボっぽくない？

