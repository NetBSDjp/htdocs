Tips of use pkgsrc

path を通す。   set path
   /usr/pkg/bin, /usr/pkg/sbin
make の入れ換え。   change make
   % sudo mv /usr/bin/make /usr/bin/make.org
   % sudo ln -s bsdmake /usr/bin/make
tar の入れ換え。   change tar
   % sudo ln -s ../../bin/gnutar /usr/pkg/bin/gtar
これらは mk ファイルで吸収すべき？   Should absorb these by the mk file?
